# GSQL
@Dialect=GOOGLE_STANDARD_SQL
CREATE TABLE users(
  user_id    INT64 NOT NULL,
  name       STRING(MAX),
  age        INT64,
) PRIMARY KEY (user_id);
===
@Dialect=POSTGRESQL
# The schema
CREATE TABLE users(
  user_id    bigint NOT NULL PRIMARY KEY,
  Name       varchar,
  Age        bigint
);
